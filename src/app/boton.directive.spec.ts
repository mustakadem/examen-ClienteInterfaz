import { BotonDirective } from './boton.directive';

describe('BotonDirective', () => {
  it('should create an instance', () => {
    const directive = new BotonDirective();
    expect(directive).toBeTruthy();
  });
});
